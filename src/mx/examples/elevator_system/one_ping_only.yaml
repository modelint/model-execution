# one_ping_only.yaml – Ping Scenario

scenario:
  name: one ping only
  description: >
    Invoke the Cabin's ping method once and check the result

interactions:

    - type: call method
      name: Ping
      class: Cabin
      domain: EVMAN
      parameters:
        dir: up
      instance:
        Shaft: S1