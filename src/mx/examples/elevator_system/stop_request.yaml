# stop_request.yaml – Request stop at some floor

scenario:
  name: stop request
  description: >
    Dispatch stop request event

interactions:

  - stimulate:
      type: model operation
      name: signal event
      event: Stop request
      domain: EVMAN
      state model : Accessible Shaft Level
      target instance:
        Floor: 3
        Shaft: S1

